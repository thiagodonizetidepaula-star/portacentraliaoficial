# Central IA v72 — limpeza técnica

- Removido o card e a integração Jogos da Copa.
- Removidas chamadas para `/api/copa`.
- Removidos arquivos e referências antigas de notícias.
- Removido código morto do painel de cotações que já não existia na interface.
- Atualizado cache-busting de `style.css` e `app.js` para `v=72`.
- Google Analytics 4 preservado em todas as páginas.
